import { Alert } from '@components/Alert';
import React from 'react';

const About = () => {
  return (
    <React.Fragment>
      <Alert namePage={'About'} />
    </React.Fragment>
  );
};

export default About;
